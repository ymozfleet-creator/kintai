# ケーニッヒ武蔵小金井 本店勤怠管理システム

**ケーニッヒ武蔵小金井 本店勤怠管理システム**は、打刻・勤怠記録・稼働時間調整・給与試算までを2ファイルで完結する、店舗向けの軽量な勤怠管理システムです。サーバー不要・GitHub Pagesでそのまま動作します。\n\n- 営業時間:11:00〜19:30(インビスは19:00まで)/定休日:火曜日 — 出勤表・シフト希望に反映済み

## 構成(2システム・4画面)

勤怠管理とシフト管理を別システムとして分離しています。データ(従業員・記録・シフト)はブラウザのlocalStorageで共有されるため、同一ドメインに置くだけで連動します。

### 勤怠管理システム
| ファイル | 役割 | 想定利用者 |
|---|---|---|
| `index.html` | 打刻画面(出勤/退勤+本日の出勤状況) | スタッフ全員 |
| `admin.html` | 勤怠管理ダッシュボード(勤怠記録・給与試算・従業員・設定/PINロック付き) | 管理者 |

### シフト管理システム
| ファイル | 役割 | 想定利用者 |
|---|---|---|
| `shift.html` | シフト表閲覧+勤務希望の申請 | スタッフ全員 |
| `shift-admin.html` | シフト管理ダッシュボード(シフト作成・承認・稼働調整/PINロック付き) | 管理者 |

### 共通
| ファイル | 役割 |
|---|---|
| `gas.gs` | スプレッドシート連携スクリプト(勤怠記録の同期・任意) |

各画面のフッターから相互に移動できます。管理者PINは両ダッシュボード共通です。

## 公開手順(GitHub Pages)

1. GitHubで新しいリポジトリを作成(例:`kintai`)。※無料プランでは Pages は Public リポジトリのみ利用可(Private リポジトリでの Pages 公開は有料プランが必要)。いずれの場合も公開URL自体は誰でもアクセス可能な点に注意
2. `index.html` `shift.html` `admin.html` `shift-admin.html` `README.md` の5ファイルをリポジトリ直下にアップロード
3. リポジトリの **Settings → Pages** を開く
4. Source を **Deploy from a branch**、Branch を **main / (root)** に設定して Save
5. 数分後に `https://<ユーザー名>.github.io/<リポジトリ名>/` で公開されます

## 初期設定

- **管理者PIN 初期値:`0000`** — 公開後、必ず admin.html →「従業員・設定」タブで変更してください
- 初回アクセス時にデモデータ(従業員5名・前月実績・当月予定シフト)が自動投入されます
- 実運用の開始時は「従業員・設定 → データ管理 → 全データ削除」→ 従業員を登録し直してください

## 主な機能

### 勤怠管理システム(index.html / admin.html)
- **打刻**:出勤/退勤ボタン。日またぎ勤務(夜勤明け退勤)対応。退勤時に休憩を自動設定(実働6h超45分/8h超60分、修正可)
- **本日の出勤(シフト+打刻状況)**:今日シフトがある人を打刻前から一覧表示。「本日のシフト 15:00〜19:30|出勤前→勤務中→退勤済」と今日の体制が打刻画面トップで即答できます
- **勤怠記録**:月別一覧・手入力追加・修正・削除・CSV出力
- **給与試算**:アルバイト=時給×実働+割増、正社員=基準内賃金÷月平均所定労働時間で残業単価を算出。時間外×1.25/深夜+0.25(変更可)
- **KPI**:本日の出勤人数/総労働/残業合計/人件費見込

### シフト管理システム(shift.html / shift-admin.html)
- **週間シフト表(日別リスト形式)**:スマホで横スクロールなしに読める日別カード。各日の見出しに「日付・曜日・◯人・合計h」、中に「● 名前 10:00–19:30 8.5h」の大きな行が開始時刻順に並び、誰が・いつ・何時から何時までが一目で分かります。火曜=定休日カード、本日=金枠+バッジ
- **スタッフ画面**:自分の行は金色+「あなた」タグ。「＋ この日に希望を出す」をタップ→時間を入れて申請(黄色の「希望」行、タップで取り消し)。「今週のあなた」サマリー付き
- **管理画面(タブなしの1画面フロー)**:「① 承認待ち(0件なら1行に自動縮小)→ ② 週間シフト表 → ③ パターン一括入力(折りたたみ)→ ④ 月間の稼働状況(要注意バッジ付き折りたたみ)」。②は行タップで変更・削除、「＋ シフトを追加」で従業員を選んで新規登録、黄色の希望行タップで即承認。週ナビの「前週をコピー」で毎週の作成が1クリック、最下部に全員の週間実働合計
- **パターン一括入力**:曜日パターン×時間帯を「表示中の週/今月/来月」へ一括適用。**前週コピー**で毎週の作成が1クリック、表示中の週のクリアも可能
- **シフト承認**:承認・却下・一括承認と履歴確認。承認待ち件数はタブにバッジ表示
- **稼働調整**:従業員ごとの月間上限(h)に対する 予定/実績/残り枠/着地予測/状態バッジ(扶養枠・36協定管理)

## スプレッドシート連携(オプション)

打刻・勤怠記録に加えて、**確定シフト**もGoogleスプレッドシートへ自動反映できます(端末内localStorageとの二重管理)。設定は勤怠管理(admin.html)の「従業員・設定→スプレッドシート連携」1か所で、シフト管理側にも自動で適用されます。設定手順は `gas.gs` 冒頭のコメントを参照してください。

- 「勤怠記録」シート:打刻・修正・削除を常に最新の状態で反映
- 「打刻ログ」シート:すべての操作履歴(追記のみ・監査ログ)
- 「確定シフト」シート:シフトの追加・変更・削除・承認・一括入力を自動反映(従業員×日付でupsert、削除は行ごと削除)。一括操作は1回の送信にまとめられます
- 同期対象外:承認前の「希望」、設定値。**方向はアプリ→シートの一方向**で、シートを編集してもアプリには反映されません。復元は「データ管理→バックアップ(JSON)」を使用してください
- 通信失敗時は端末内のキューに保持され、回復後に自動再送されます(どの画面を開いていても再送)
- 連携を後から有効にした場合:記録は「全記録を一括送信」、シフトはシフト管理③の「確定シフトをスプレッドシートへ一括送信」(先月〜来月分)で反映できます

## データの保存について(重要)

- データは**ブラウザのlocalStorage**に保存されます。`index.html` と `admin.html` は同一ドメインに置くことでデータを共有します
- **端末・ブラウザを変えるとデータは引き継がれません**。打刻用の端末を1台に固定して運用してください
- 定期的に「データ管理 → バックアップ(JSON保存)」でバックアップを取得してください
- PINロックは簡易的な画面制御であり、厳密なセキュリティではありません。複数端末運用や本格的な保護が必要になった場合は Firebase 等のサーバー認証型への移行を推奨します

## 免責

給与額はあくまで試算です。週40時間超の残業判定・社会保険・各種控除は含まれないため、給与計算の確定値には使用しないでください。
